name: 2013 Football Schedule Checker
 
on:
  workflow_dispatch:
 
jobs:
  check-schedules:
    runs-on: ubuntu-latest
 
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
 
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
 
      - name: Install Chrome
        run: |
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
 
      - name: Install Python dependencies
        run: pip install selenium requests beautifulsoup4 pandas webdriver-manager
 
      - name: Run schedule checker
        run: python Schedule_Checker.py
 
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: schedule-checker-results
          path: |
            mshsaa_missing_games.csv
            mshsaa_school_ids.csv
